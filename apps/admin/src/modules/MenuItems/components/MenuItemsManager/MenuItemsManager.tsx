import { MenuItemsManagerProps } from './types';

const MenuItemsManager = ({ menuId }: MenuItemsManagerProps) => {
  return <div>...MenuItemsManager...{menuId}</div>;
};

export default MenuItemsManager;
